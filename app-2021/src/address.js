// NOTE: we still need the JSDoc below because WebStorm is not picking up the
// types of the properties from the address.d.ts file. VSC is doing it
// correctly.

export class address {
  /**
   * @type number
   */
  id;

  /**
   * @type string
   */
  firstName;

  /**
   * @type string
   */
  lastName;
}
